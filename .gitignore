# Python
.venv/
__pycache__/
*.pyc

# Heavy data — never commit
video_clips/
output_videos/
stubs/
frames_to_label/
datasets/
calibrations/*.json

# Analytics DBs — local-only artefacts, contain match data
data/
*.db
*.db-shm
*.db-wal

# Model weights — too big for git, use Releases / LFS / Drive
models/*.pt
*.pt

# Notebooks runtime artefacts
notebooks/runs/
notebooks/wandb/
*.cache

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/launch.json